# Define the list
list = [1, 2, 3, 4, 5]

# Get the length of the list
length = len(list)

# Use a for loop to iterate over the list in reverse order
for i in range(length - 1, -1, -1):
    print(list[i])
